# Post-Release Metrics

Snapshot date: 2026-05-12

## Public Package and Repository

| Metric | Value | Source |
| --- | ---: | --- |
| npm latest version | `0.2.1` | `npm view @coratch/agent-session-viewer dist-tags --json` |
| GitHub stars | `0` | `gh repo view Coratch/agent-session-viewer --json stargazerCount` |
| GitHub forks | `0` | `gh repo view Coratch/agent-session-viewer --json forkCount` |
| GitHub watchers | `0` | `gh repo view Coratch/agent-session-viewer --json watchers` |
| npm weekly downloads | unavailable | npm downloads API returned `package @coratch/agent-session-viewer not found` on 2026-05-12 |

## Notes

The npm package exists and `latest` points to `0.2.1`, but the npm downloads API did not return a weekly count for the scoped package during this snapshot. Recheck after the package has aged past the first reporting window.
